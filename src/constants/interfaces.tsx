export interface ProbabilityItem{
    X: number,
    P: number
}

export interface Point {
    x:number,
    y:number
}

export interface geometryData{
    m:number,
    n:number,
    M:number,
    N:number
}